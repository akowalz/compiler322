(((edi <- 49)
  (esi <- 99)
  (ecx <- edi < esi)
  (edi <- ecx)
  (edi *= 2)
  (edi += 1)
  (eax <- (print edi))
  (ecx <- esi < edi)
  (edi <- ecx)
  (edi *= 2)
  (edi += 1)
  (eax <- (print edi))
))

