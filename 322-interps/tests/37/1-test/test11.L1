(((call :eax)
  (call :ebx)
  (call :ecx)
  (call :edx)
  (call :esi)
  (call :edi))
  (:eax
   (ebx <- 1)
   (eax <- (print ebx))
   (return))
  (:ebx
   (ebx <- 3)
   (eax <- (print ebx))
   (return))
  (:ecx
   (ecx <- 5)
   (eax <- (print ecx))
   (return))
  (:edx 
   (edx <- 7)
   (eax <- (print edx))
   (return))
  (:esi 
   (ebx <- 9)
   (eax <- (print ebx))
   (return))
  (:edi 
   (ebx <- 11)
   (eax <- (print ebx))
   (return))
)
