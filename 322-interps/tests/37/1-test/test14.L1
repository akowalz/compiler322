(((call :L1)
  (edx <- (mem eax 4))
  (eax <- (print edx))
  (call :L2)
  (edx <- (mem eax 4))
  (eax <- (print edx)))
  (:L1
  (edx <- 45)
  (eax <- (allocate edx 3))
  (ecx <- 41)
  (ecx *= 3)
  ((mem eax 4) <- ecx)
  (return))
  (:L2
   (edx <- 45)
   (eax <- (allocate edx 3))
   (ecx <- 25)
   (ecx += 2)
   ((mem eax 4) <- ecx)
   (return)))
