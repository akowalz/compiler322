(((call :a)
  (call :b))
 (:a
  (eax <- 1)
  (eax <- 1)
  (eax <- 1)
  (eax <- 1)
  (eax <- 1)
  (eax <- 1)
  (eax <- 1)
  (eax <- (print eax))
  (return))
 (:b
  (eax <- 3)
  (eax <- 3)
  (eax <- 3)
  (eax <- 3)
  (eax <- 3)
  (eax <- 3)
  (eax <- 3)
  (eax <- (print eax))
  (return))
)
