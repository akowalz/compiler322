(((eax <- 1)
  ((mem ebp -12) <- 3)
  ((mem ebp -16) <- 5)
  ((mem ebp -20) <- 7)
  ((mem ebp -24) <- 9)
  (edi <- 11)
  (esi <- 13)
  (eax <- (print eax))
  (ebx <- (mem ebp -12))
  (eax <- (print ebx))
  (ebx <- (mem ebp -16))
  (eax <- (print ebx))
  (ebx <- (mem ebp -20))
  (eax <- (print ebx))
  (ebx <- (mem ebp -24))
  (eax <- (print ebx))
  (eax <- (print edi))
  (eax <- (print esi))))
