(((ecx <- 5)
  (ebx <- 7)
  (edx <- 3)
  :loop
  (ecx += ebx)
  (ecx -= edx)
  (ecx *= edx)
  ((mem ebp -12) <- ecx)
  ((mem ebp -16) <- edx)
  (eax <- (print ecx))
  (ecx <- (mem ebp -12))
  (edx <- (mem ebp -16))
  (cjump ecx < 1001 :loop :done)
  :done
  (eax <- (print ebx))))
