((let ((comp (< 0 1)))
 (if comp
 (let ((t (new-tuple 3 4)))
   (let ((r (:a 2 t)))
        (print r)))
 (print 0)))
 (:a
  (x results)
  (let ((y (aref results 0)))
        (let ((z (aref results 1)))
          (let ((v1 (+ x y)))
                (let ((v2 (+ x z)))
                        (new-tuple v1 v2)))))))
