(
	(
		(call :foo)
	)
	(
		:foo
		(ebx <- 5)
		(ecx <- 7)
		(ebx += ecx)
		(ebx -= 1)
		(eax <- (print ebx))
		(ebx += 2)
		(eax <- (print ebx))
		(ecx <- 7)
		(ebx -= ecx)
		(ebx += 1)
		(eax <- (print ebx))
		(ebx -= 2)
		(eax <- (print ebx))
		(ecx <- 3)
		(ebx *= ecx)
		(eax <- (print ebx))
		(ebx *= 3)
		(ebx -= 2)
		(eax <- (print ebx))
		(return)
	)
) 
