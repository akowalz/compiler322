;this is to test the cmp
((
  (ebx <- 9)
  (esi <- 9)
  (edi <- -15)
  (eax <- ebx <= esi)
  (ecx <- ebx < edi)
  (edx <- edi < ebx)

  (ebx <- eax)
  (esi <- ecx)
  (edi <- edx)

  (ebx *= 2)
  (ebx += 1)
  (eax <- (print ebx))

  (esi *= 2)
  (esi += 1)
  (eax <- (print esi))

  (edi *= 2)
  (edi += 1)
  (eax <- (print edi))
))
