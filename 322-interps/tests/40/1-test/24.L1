((
  (eax <- 3)
  (eax += 4)
  (eax <- (print eax))

  (eax <- 3)
  (ebx <- 4)
  (eax += ebx)
  (eax <- (print eax))

  (eax <- 5)
  (ebx <- 89)
  (eax *= ebx)
  (eax <- (print eax))

  (eax <- 6)
  (ebx <- 3)
  (eax -= ebx)
  (eax <- (print eax))
))
