(((call :L_4))
 (:L_4 (ecx <- 43) (tail-call :f))
 (:f
  (x <- ecx)
  (ecx <- x)
  (edx <- x)
  (call :g)
  (x_1 <- eax)
  (eax <- x_1)
  (eax += 2)
  (return))
 (:g
  (x <- ecx)
  (y <- edx)
  (ecx <- x)
  (edx <- y)
  (eax <- y)
  (call :h)
  (x_2 <- eax)
  (eax <- x_2)
  (eax += 2)
  (return))
 (:h
  (x <- ecx)
  (y <- edx)
  (z <- eax)
  (eax <- (print x))
  (d1 <- eax)
  (eax <- (print y))
  (d2 <- eax)
  (eax <- (print z))
  (eax <- eax)
  (return)))
