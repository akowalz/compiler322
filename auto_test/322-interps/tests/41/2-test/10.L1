(((eax <- 3)
  (ebx <- 7)
  (ecx <- 35)
  (edi <- 17)
  (edx <- 501)
  (esi <- 3)
  ((mem ebp -4) <- 22)
  (ebx += eax)
  (ecx += ebx)
  (edi += ecx)
  (edx += edi)
  (esi += edx)
  (ebx <- (mem ebp -4))
  (ebx += esi)
  ((mem ebp -4) <- ebx)
  (eax += ebx)
  (eax <- (print eax))))
