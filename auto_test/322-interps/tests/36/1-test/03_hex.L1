((
  (edi <- 255) ;; 0xff
  (esi <- edi)
  (edi *= 2)
  (edi += 1)
  (eax <- (print edi))
  (edi -= esi)
  (edi -= 1)
  (edi <<= 8)
  (edi += 255) ;; 0xffff
  (edi *= 2)
  (edi += 1)
  (eax <- (print edi))
  (edi -= esi)
  (edi -= 1)
  (edi <<= 8)
  (edi += 255) ;; 0xffffff
  (edi *= 2)
  (edi += 1)
  (eax <- (print edi))
  (edi -= esi)
  (edi -= 1)
  (edi <<= 8)
  (edi += 255) ;; 0xffffffff
  (edi *= 2)
  (edi += 1)
  (eax <- (print edi))
  (edi -= esi)
  (edi -= 1)
  (edi += 1)
  (edi *= 2)
  (edi += 1)
  (eax <- (print edi))
  (edi -= esi)
  (edi -= 1)
  ))
  