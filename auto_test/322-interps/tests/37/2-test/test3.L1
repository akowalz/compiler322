(((eax <- 1)
  (ebx <- 1)
  (ecx <- 1)
  (edx <- 1)
  (esi <- 1)
  (edi <- 1)
  ((mem ebp -4) <- 2)
  (eax += eax)
  (eax += ebx)
  (eax += ecx)
  (eax += edx)
  (eax += esi)
  (eax += edi)
  (ebx <- (mem ebp -4))
  (eax += ebx)
  (eax <- (print eax))
))
