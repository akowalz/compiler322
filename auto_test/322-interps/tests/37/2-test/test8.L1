(((eax <- (allocate 9 0))
  (eax += 4)
  (ebx <- 1)
  :loop
  ((mem eax 0) <- ebx)
  (ebx += 2)
  (ecx <- (mem eax 0))
  (eax += 4)
  ((mem ebp -12) <- eax)
  ((mem ebp -16) <- ebx)
  ((mem ebp -20) <- ecx)
  (eax <- (print ecx))
  (eax <- (mem ebp -12))
  (ebx <- (mem ebp -16))
  (ecx <- (mem ebp -20))
  (cjump ebx < 9 :loop :done)
  :done))
