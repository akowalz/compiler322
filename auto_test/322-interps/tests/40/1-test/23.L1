((
  (eax <- (allocate 89 7)) 

  (edi <- eax)
  (esi <- (mem edi 4)) ; put the first element into esi
  (eax <- (print esi)) ; print it

  (esi <- (mem edi 176)) ;put the last element into esi
  (eax <- (print esi)) ; print it

))
