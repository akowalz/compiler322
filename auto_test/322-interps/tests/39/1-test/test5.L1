(((esp -= 8)
  (ecx <- 201)
  (eax <- 129)
  (eax -= 1)
  (eax <<= 6)
  (eax += 1)
  ((mem esp 0) <- eax)
  ((mem esp 4) <- ecx)
  (eax <- (print eax))
  (ecx <- (mem esp 4))
  (eax <- (mem esp 0))
  (eax -= 1)
  (eax >>= 6)
  (eax += 1)
  ((mem esp 0) <- eax)
  ((mem esp 4) <- ecx)
  (eax <- (print eax))
  (ecx <- (mem esp 4))
  (eax <- (mem esp 0))
  ((mem esp 0) <- eax)
  ((mem esp 4) <- ecx)
  (eax <- (print ecx))
  (ecx <- (mem esp 4))
  (eax <- (mem esp 0))
  (esp += 8)))