((let ((t (new-tuple 3 4)))
   (let ((r (:a 2 t)))
     (let ((x (print r)))
 	(let ((t2 (new-tuple 3 4)))
   	  (let ((r2 (:a 3 t2)))
        	(print r2))))))
 (:a
  (x results)
  (let ((y (aref results 0)))
        (let ((z (aref results 1)))
          (let ((v1 (+ x y)))
                (let ((v2 (+ x z)))
                        (new-tuple v1 v2)))))))
